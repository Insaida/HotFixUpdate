---
      - hosts: aws-ec2
        remote_user: ec2-user
        connection: ssh

        tasks:
            - name: Yum packages Update
              yum:
                  name: "*"
                  state: latest
                  








